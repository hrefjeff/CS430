#include <iostream>
#include <vector>
using namespace std;

void compare(vector<bool>, vector<bool>);

int main()
{

    vector<bool> list;
    vector<bool> other_list;

    list.resize(3);
    other_list.resize(3);

    return 0;
}

void compare(vector<bool> list_one, vector<bool> list_two)
{
    
}